{
  "name": "multicodex-mac",
  "private": true,
  "version": "0.1.0",
  "description": "Native macOS menu bar app for multicodex",
  "scripts": {
    "build": "swift build",
    "typecheck": "swift build",
    "test": "swift build",
    "dev": "swift run MultiCodex"
  }
}
